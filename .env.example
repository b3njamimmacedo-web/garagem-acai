# Copie para .env e preencha. O .env está no .gitignore.

# Credenciais da Asimov Academy (opcionais: se vazias, você digita no navegador)
ASIMOV_EMAIL=
ASIMOV_PASSWORD=

# Chave da API da Anthropic (ou use `ant auth login`)
ANTHROPIC_API_KEY=
